no sql is when we dont define any schema and we can store unstructured data.
- for ex, if we misspell a field name it will just create a new field instead of throwing an error. where as in sql if we misspell a field name it will throw an error.
