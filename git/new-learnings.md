 `git config --global core.pager ""` - it disables pagination in git commands, allowing for continuous output without pauses.(disabled vim for me)

`git switch <branch-name>`

`git push origin --delete <branch-name>`
`git branch -d <branch-name>`

`git reset --hard`

`git config --global core.editor "code --wait"`